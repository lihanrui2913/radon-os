[package]
name = "nvmed"
version = "0.1.0"
edition = "2024"

[dependencies]
libdriver = { path = "../../libdriver" }
libradon = { path = "../../libradon" }
pcid = { path = "../pci" }
radon_kernel = { path = "../../kernel" }
spin = "0.10.0"
